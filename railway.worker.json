{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --prefix services/band-workers"
  },
  "deploy": {
    "startCommand": "npm start --prefix services/band-workers",
    "restartPolicyType": "ALWAYS"
  }
}
