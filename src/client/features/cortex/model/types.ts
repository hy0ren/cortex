export type CortexScreen = "intake" | "pipeline" | "report" | "history";

export type { FlagSeverity, GliaFlag, WaveBar } from "@/data/contracts";

export type NavStyle = {
  bg: string;
  bar: string;
  col: string;
};
